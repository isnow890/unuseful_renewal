import 'package:flutter/material.dart';

class HitDutyLogScreen extends StatelessWidget {
  const HitDutyLogScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {



    return Center(
      child: Text('log'),
    );
  }
}
