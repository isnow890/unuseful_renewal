import 'package:flutter/material.dart';

class HitScheduleSettingScreen extends StatelessWidget {
  const HitScheduleSettingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child:
      Text('settings'),);
  }
}
